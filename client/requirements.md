## Packages
recharts | For the disease distribution pie charts and analytics
framer-motion | For smooth page transitions and micro-interactions
date-fns | For formatting dates and timestamps

## Notes
- Tailwind config needs to extend font family for 'Outfit' and 'DM Sans'
- API calls use credentials: "include" for auth
- AI features endpoint: /api/ai/predict and /api/ai/assign
